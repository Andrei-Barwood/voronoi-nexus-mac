# Examples

- `inputs/` - Sample crypto data JSONs
- `batch-process.sh` - Batch analyzer for multiple files
- `library-usage/` - Example of consuming EllipticProof as a library

Run `./examples/batch-process.sh` or `cd examples/library-usage && swift run LibraryDemo`
