# Library Usage Example

Demonstrates using `EllipticProof` as a library in another Swift package.

## Run

```bash
cd examples/library-usage
swift run LibraryDemo
```

## Why useful

Embed the crypto analyzer directly in apps, tools, or pipelines without spawning the CLI.
